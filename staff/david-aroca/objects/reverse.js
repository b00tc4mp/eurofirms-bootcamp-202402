function reverseElement(object) {
    var iterations = math.floor(object.length / 2)

    for (var i = 0; i < object.length / 2; i++) {
        const element = object[i]

        object[i] = object[object.length - 1 - i]

        object[object.length - 1 - i] = element

    }

}

console.log('FUNCTION REVERSE')

    ('CASE 1 : reverse the animal object ')


var animals = {
    0: 'perro',
    1: 'gato',
    2: 'pajaro',
    3: 'leon',
    4: 'tigre',
    length: 5
}
var result = reverse(series)
console.log(result)
console.log(animals)
console.log(sameObjects: result === series)
    ('CASE 1 : Using the function reverse  put the first element on the last and the last on the first ')

var cities = {
    0: 'Badajoz',
    1: 'Valencia',
    2: 'Madrid',
    3: 'Barcelona',
    length: 4
}















    ('CASE 1 : Using the function reverse  put the first element on the last and the last on the first ')