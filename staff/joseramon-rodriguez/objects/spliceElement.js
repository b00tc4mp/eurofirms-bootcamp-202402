function spliceElement(object, start, deleteCount, items) {
    var result = { length: 0 }
    var startIndex = start
    if (-object.length <= start < 0)
        startIndex = start + object.length
    if (start < -object.length)
        startIndex = 0
    if (start >= object.length) {
        //add elements to objects
    }
    //comprobar si existe start
    if ()






        return result
}